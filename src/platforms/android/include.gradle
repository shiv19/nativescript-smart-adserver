/* Include.gradle configuration: http://docs.nativescript.org/plugins/plugins#includegradle-specification */

android {
	productFlavors {
		"nativescript-smart-adserver" {
			dimension "nativescript-smart-adserver"
		}
	}
}

dependencies {
    // def googlePlayServicesVersion = project.hasProperty('googlePlayServicesVersion') ? project.googlePlayServicesVersion : '+'
	// compile "com.google.android.gms:play-services:$googlePlayServicesVersion"
}